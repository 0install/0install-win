<Project DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003" ToolsVersion="3.5">
  <PropertyGroup>
    <!-- The configuration and platform will be used to determine which
         assemblies to include from solution and project documentation
         sources -->
    <Configuration Condition=" '$(Configuration)' == '' ">Debug</Configuration>
    <Platform Condition=" '$(Platform)' == '' ">AnyCPU</Platform>
    <SchemaVersion>2.0</SchemaVersion>
    <ProjectGuid>{fdeb595f-97a2-4479-b6f0-febf9760c0e4}</ProjectGuid>
    <SHFBSchemaVersion>1.8.0.0</SHFBSchemaVersion>
    <!-- AssemblyName, Name, and RootNamespace are not used by SHFB but Visual
         Studio adds them anyway -->
    <AssemblyName>Twilight</AssemblyName>
    <RootNamespace>Core</RootNamespace>
    <Name>Game</Name>
    <!-- SHFB properties -->
    <OutputPath>build\Backend\Documentation</OutputPath>
    <HtmlHelpName>ZeroInstall</HtmlHelpName>
    <PresentationStyle>hana</PresentationStyle>
    <IndentHtml>False</IndentHtml>
    <HelpTitle>Twilight Symphony</HelpTitle>
    <DocumentationSources>
      <DocumentationSource sourceFile="build\Backend\Debug\Common.dll" />
      <DocumentationSource sourceFile="build\Backend\Debug\Common.xml" />
      <DocumentationSource sourceFile="build\Backend\Debug\ZeroInstall.Model.dll" />
      <DocumentationSource sourceFile="build\Backend\Debug\ZeroInstall.Model.xml" />
      <DocumentationSource sourceFile="build\Backend\Debug\ZeroInstall.Store.dll" />
      <DocumentationSource sourceFile="build\Backend\Debug\ZeroInstall.Store.xml" />
  </DocumentationSources>
  </PropertyGroup>
  <!-- There are no properties for these two groups but they need to appear in
       order for Visual Studio to perform the build. -->
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Debug|AnyCPU' ">
  </PropertyGroup>
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Release|AnyCPU' ">
  </PropertyGroup>
  <!-- Import the SHFB build targets -->
  <Import Project="$(SHFBROOT)\SandcastleHelpFileBuilder.targets" />
</Project>
